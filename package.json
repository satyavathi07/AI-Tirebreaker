{
  "name": "the-tiebreaker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx http-server . -p 8080",
    "dev": "npx live-server --port=8080 --entry-file=index.html"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "live-server": "^1.2.2"
  }
}
